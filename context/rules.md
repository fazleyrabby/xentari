# RULES
- Return ONLY valid unified diffs for code changes.
- NO explanations or prose in LLM outputs.
- Must include "diff --git" and "@@" headers.
- Adhere to the detected model tier constraints (Small, Medium, Large).
- Maintain project structure: models/, services/, routes/, controllers/.
